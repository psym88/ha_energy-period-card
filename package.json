{
  "name": "ha_energy-period-card",
  "version": "1.1.1",
  "private": true,
  "description": "Energy period selector card for Home Assistant",
  "scripts": {
    "test": "node tests/card.test.mjs",
    "check": "node --check ha_energy-period-card.js && npm test"
  }
}
