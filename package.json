{
  "name": "ha-energy-period-card",
  "version": "1.0.0",
  "private": true,
  "description": "Energy period selector card for Home Assistant",
  "scripts": {
    "build": "node scripts/build.mjs",
    "check": "npm run build && node --check dist/ha_energy-period-card.js"
  }
}
